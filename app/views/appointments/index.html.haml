.container
  %h1 Visitas
  %hr
  = button_to 'Novo', new_appointment_path, method: :get

  - if @appointments.empty?
    %p Nada encontrado.
  - else
    %table
      %th Descrição
      %th Morador
      %th Visitante
      %th Data
      - @appointments.each do |appointment|
        %tr
          %td= link_to appointment.description, appointment
          %td= link_to appointment.host.name, appointment.host
          %td= link_to appointment.visitor.name, appointment.visitor
          %td= appointment.date
  %p
    Total:
    = @appointments.count
