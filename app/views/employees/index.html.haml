%p#notice= notice

%h1
  Funcionários
  = link_to ?+, new_employee_path

- if @employees.empty?
  %p Nada encontrado.
- else
  .container
    %table
      %th Nome:
      %th Cpf:
      %th Empresa:
      - @employees.each do |employee|
        %tr
          %td= link_to employee.name, employee
          %td= employee.cpf
          %td= link_to employee.company.name, employee.company
